# Game Chooser

Simple Progressive Web App to randomly choose an option from a list.

Used mainly for my pondering of games to peruse from on a daily matter.

## Running locally

Just serve the PWA with npx:

`npx serve .`

And then open the app in your browser, typically under http://localhost:3000
